// create the data models, those models con be used inside state or components

// e.g:

/*
export type Todo = {
  id: string;
  title: string;
  completed: boolean;
};

export enum TodosFilter {
  All = '',
  Completed = 'completed',
  Active = 'active',
}

*/